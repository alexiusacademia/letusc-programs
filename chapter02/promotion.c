/*
    Program that demostracts the number type conversion.
    This programs tries to promote the type of the given value.
*/

#include <stdio.h>
int main(int argc, char const *argv[])
{
    float a;
    a = 9;

    printf("a now is %f\n", a);

    return 0;
}
